Approximation of FP Multiplication with MBE
Neurons
1. 개별 피연산자를 스파이크 시퀀스로 표현 (Approximate
Identity Mapping)
먼저, 곱셈의 두 피연산자, 즉 x1과 x2를 각각 MBE 뉴런을 사용하여 스파이크 시퀀스로 변환
한다. 이 때, MBE 뉴런은 입력값 x를 거의 그대로 (approximate identity mapping) 스파이크
신호로 변환하는 역할을 한다.
x≈ M BEId(x)=
T−1
∑
t=0
d[t]s[t]
2. 스파이크 시퀀스를 이용한 곱셈 근사
개별 피연산자 x1 과 x2 가 스파이크 시퀀스 s와 강도 벡터 d로 표현되었다면, 이제 이 스파이
크 표현들을 이용하여 x1⋅x2 를 근사한다.
x1⋅x2 ≈ (
T−1
∑
i=0
d[i]s[i])⋅(
T−1
∑
j=0
d′[j]s′[j])
이 식을 전개하면 다음과 같은 형태가 된다. 여기서 d와 s는$ x1에대한것이고,d′와 s′는 x2에
대한 것이다.
$$x_1 \cdot x_2 \approx \sum {i=0}^{T-1} \sum _{j=0}^{T-1} d[i]d'[j] \cdot s[i]s'[j]$$
이들을 이용하여 다음과 같은 행렬을 정의할 수 있다.
x는 입력값 (float)이다.
T는 총 타임스텝(timestep) 수이다.
s[t]∈ 0,1은 각 타임스텝 t에서 생성되는 바이너리 스파이크이다.
d[t]는 각 타임스텝 t에서 스파이크의 강도(intensity)를 나타내는 값이다.
∑T−1t=0d[t]s[t]는 T개의 타임스텝에 걸쳐 생성된 스파이크들의 가중치 합으로, 원래 입력
값 x를 근사한다.
즉, 원래의 연속적인 부동소수점 값 x를 T개의 타임스텝 동안 발생하는 스파이크들의 시
퀀스와 각 스파이크의 강도로 표현하는 것이다. 이 과정 자체는 MBE 뉴런의 근사 능력
을 활용한다.
강도 행렬 (Intensity Matrix) D∈ RT×T:
Dij=d[i]d′[j]
이는 d와 d′의 외적(outer product)으로 볼 수 있다. D=dTd′(여기서 dT는 d의 전치 행렬
이다).
스파이크 행렬 (Spike Matrix) S∈ {0,1}T×T:
이제 원래의 곱셈 근사식은 다음과 같이 표현된다.
x1⋅x2 ≈
T−1
∑
i=0
T−1
∑
j=0
Dij⋅Sij= (D⊙S)sum
여기서D⊙S는 두 행렬의 요소별 곱(Hadamard product)을 의미하며, (D⊙S)sum은 그 결과
행렬의 모든 요소의 합을 나타낸다.
의의
이 방식의 가장 큰 장점은 최종적인 곱셈 연산이 스파이크 행렬 S(바이너리 값)와 미리 계산
될 수 있는 강도 행렬 D(최대 N×N 개의 d 값 조합으로 구성될 수 있으며, N은 뉴런의 총
개수)를 사용하여 수행된다는 점이다. 즉, FP 곱셈이 필요한 모든 곳에서 스파이크 기반의 계
산으로 대체된다.
강도 행렬 D는 한 번 계산되어 네트워크 전체에서 공유될 수 있다.
실제 연산은 스파이크 행렬 S (희소할 수 있음)와 D의 요소별 곱셈 및 합산으로 이루어지므
로, FP 곱셈보다 에너지 효율성이 훨씬 높다.
Sij=s[i]s′[j]
이는 s와 s′의 외적(outer product)으로 볼 수 있으며, Hadamard product sTs′ 와 같다.
S=sTs′